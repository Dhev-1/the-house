# AUTOGENERATED by cloon/newdot/house scripts/apply-theme.sh — do not edit by hand.
# Rewritten whenever a theme is chosen in the house bar's picker; sourced from
# hyprland.conf so the choice sticks across a restart. The $-variables are here
# for reuse (e.g. from binds or window rules); the keywords below are what paint.
#
# Shipped state: the Noir table, so a fresh install deals gold before the shell
# ever writes this file itself.

$surface = rgb(0e0b0d)
$text    = rgb(e8ddc4)
$subtext = rgb(8a7f6d)
$accent  = rgb(d4af5f)
$idle    = rgb(2a2320)
$urgent  = rgb(c13a4e)

general:col.active_border = rgba(d4af5fee)
general:col.inactive_border = rgba(2a232000)
decoration:shadow:color = rgba(0e0b0dee)
